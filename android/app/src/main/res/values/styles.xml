<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash-screen theme shown while Flutter engine initialises. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@android:color/black</item>
        <!-- Allow the Flutter surface to draw under the status bar. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <!-- Theme applied once the Flutter engine has started. Flutter controls
         window insets via SystemChrome, so this just needs to be transparent. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>
</resources>
