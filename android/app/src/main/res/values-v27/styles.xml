<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- API 27+ override: opt into display-cutout (notch) rendering so the
         full-screen camera preview extends into the status-bar area on all
         modern devices. Requires android:windowLayoutInDisplayCutoutMode
         which is only available from API 28, but the parent theme handles
         the translucent bars on API 27. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>
</resources>
